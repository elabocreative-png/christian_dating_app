---
description: Flutter coding standards for ChristMeets — what to do now vs. what is planned
globs: lib/**/*.dart
alwaysApply: false
---

# Flutter Standards — ChristMeets

These are the conventions to follow **right now**. See `architecture.mdc` for the
folder layout and layering rules.

## Enforced now

1. **Feature-first structure.** New code goes under `lib/features/<name>/`
   (`data/` · optional `domain/` · `presentation/`), or `lib/core/` if it is shared by
   2+ features. Never add another feature's internals as a dependency.
2. **No business logic in the UI.** Widgets render and dispatch; they do not contain
   Firestore queries, filtering rules, or calculations. Put data access in a repository
   (`data/`) and pure logic in `domain/`.
3. **Trimmed layers, not full Clean Architecture.** Use `data/ · domain/ · presentation/`.
   Do **not** add `entities/`, `usecases/`, or repository interfaces — this is a
   single-source (Firebase) app and that boilerplate is not warranted.
4. **State management: Riverpod.** Use `flutter_riverpod` for shared/session and
   server-backed state (`StreamProvider`, `FutureProvider`, `Provider`, `NotifierProvider`).
   Use `ConsumerWidget` / `ConsumerStatefulWidget` where widgets need `ref`. Keep
   `setState` for purely local UI state (animations, text controllers, toggles).
5. **Provider placement.** Feature repositories live in `features/<name>/data/` with a
   `*RepositoryProvider`. Cross-screen providers go in
   `features/<name>/presentation/*_providers.dart`. Session/auth providers live in
   `features/auth/presentation/auth_providers.dart` (`authStateProvider`,
   `currentUserIdProvider`, `profileCompleteProvider`).
6. **No Firestore in presentation or domain.** Import `cloud_firestore` only in
   `features/*/data/`. Parse timestamps in pure code via `firestore_value_utils.dart`.
7. **Navigation: `Navigator` + `rootNavigatorKey`** (`core/navigation/app_navigator.dart`).
8. **Imports use `package:christian_dating_app/...`** — no cross-folder relative `../` imports.
9. **Prefer `const` constructors** and keep widgets small; split feature-specific widgets
   out rather than growing a mega-widget.

See `AGENTS.md` for repository/provider map and agent workflow.

## Planned — do NOT introduce unsolicited

The following are intended directions but are **not adopted yet**. Do not add them to new
code unless the change is explicitly a migration to that tool, otherwise the codebase ends
up with two competing patterns.

- **GoRouter** for routing / auth gate / deep links — planned; until then use `Navigator`.
- **Repository interfaces / entities / use-cases** — only if the app gains a second data
  source or genuinely complex domain logic.

When you think one of these should finally be adopted, say so and propose a deliberate
migration — do not start mixing it in piecemeal.
